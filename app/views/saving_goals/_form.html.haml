= simple_form_for(@saving_goal) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :description
    = f.input :goal_date
    = f.input :amount

  .form-actions
    = f.button :submit
